# `src/app/(dashboard)/`

Rutas y layouts para la parte **privada/interna** (panel, settings, administración).

## Convenciones

- Autorización/autenticación se resuelve desde `src/features/auth/` y/o `src/lib/auth.ts`.
- Preferir Server Components por defecto; usar Client Components sólo cuando sea necesario.

